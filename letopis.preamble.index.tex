% vim: keymap=russian-jcukenwin
%%beginhead 
 
%%file preamble.index
%%parent preamble
 
%%endhead 

%\usepackage{makeidx}
%\usepackage[xindy]{imakeidx}
%\usepackage{imakeidx}
%\usepackage{sectsty}

%\usepackage{index}

%\newindex{ppl}{idx_ppl}{ind_ppl}{People Index}

\ifINDEX
  \makeindex

	\makeatletter
	\newcommand{\rindex}[2][\imki@jobname]{%
	  \index[#1]{\detokenize{#2}}%
	}
	\makeatother
	
	%\makeindex[name=index.eng,title=Index,options=-M index.eng.xdy]
	%\makeindex[name=index.rus,title=Предметный указатель,options=-M index.rus.xdy]
	%\makeindex[name=index.names.rus,title=Именной указатель,options=-M index.rus.xdy]

	\makeindex[name=index.eng,title=Index]
	\makeindex[name=index.rus,title=Предметный указатель]
	\makeindex[name=index.names.rus,title=Имена]
	\makeindex[name=index.cities.rus,title=Города]

\fi
